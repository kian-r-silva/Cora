import React from 'react';

const Renderer = () => {
    return (
        <div>
            {/* Render notebook content here */}
            <h1>Notebook Renderer</h1>
        </div>
    );
};

export default Renderer;